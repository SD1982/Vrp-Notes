{% extends 'base.html.twig' %}

{% block title %}{% endblock %}

{% block body %}
    <h1>Note du
        {{ note.date | date('d/m/Y') }}</h1>

    {{ note.montant }}
    {{ note.type }}
    {{ note.adress }}
    {{ note.postcode }}
    {{ note.city }}
    {{ note.country }}
    {{ note.latitude }}
    {{ note.longitude }}
    {{ note.description }}
{% endblock %}